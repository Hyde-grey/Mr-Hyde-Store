const LoginForm = () => {
  return (
    <div>
      <div>
        <h2>already have an account ?</h2>
        <p>Signin with you email and password</p>
      </div>
      <form action="">
        <input type="text" placeholder="Email" />
        <input type="password" placeholder="Password" />
      </form>
    </div>
  );
};

export default LoginForm;
